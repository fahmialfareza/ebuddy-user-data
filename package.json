{
  "name": "ebuddy-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "start-backend": "pnpm --filter=backend-repo start",
    "start-web": "pnpm --filter=frontend-repo start",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.4.0",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@8.0.0",
  "engines": {
    "node": ">=22"
  }
}
